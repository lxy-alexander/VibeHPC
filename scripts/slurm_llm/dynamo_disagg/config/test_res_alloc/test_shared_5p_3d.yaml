# Test case 1: SHARED allocation
# 8 GPU node, 5 prefill × 1 GPU, 3 decode × 1 GPU
# Expected: SHARED mode, 1 node per cluster

benchmark: deepseek-r1
scenario: Interactive

disagg_cluster:
  num_prefill_workers: 5
  num_decode_workers: 3

prefill:
  system: Test_Systemx1
  gpus_per_worker: 1
  gpus_per_node: 8
  config: /work/test/prefill.yaml

decode:
  system: Test_Systemx1
  gpus_per_worker: 1
  gpus_per_node: 8
  config: /work/test/decode.yaml
